using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PageManager : MonoBehaviour
{
    // items könnten einen static index abgeben sobald sie aufgesammelt wurden 
    // und sobald der index die zahl einmal erreicht hat, wird der zugewiesene Wert auf True 
    // gesetzt und so getriggert, dass das Lock Image verschwindet 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
